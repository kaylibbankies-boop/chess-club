"""
Chess Club Serializers (Not needed in Flask)
In Flask with SQLAlchemy, we use the to_dict() methods in the models
instead of separate serializer classes like in Django REST Framework.

This file is kept for reference but functionality is now in models.py
"""
